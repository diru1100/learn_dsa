#include<stdc++.h>

using namespace std;

int main(){

    int truckSpace = 90;
    truckSpace -= 30;

    int numPackages = 5;

    vector<int> packageSpace[20,30,35,35,60];

    unordered_map<int, int> spaceToIndex;

    for (int i =0; i < numPackages; i++ ){

        int val = packageSpace[i];

        if(spaceToIndex[truckSpace-val]){


        }
        if
        spaceToIndex[truckSpace-val] =
        }


        if 
    }

    return 0;
}